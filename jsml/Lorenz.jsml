component Lorenz
  variable x::Real
  variable y::Real
  variable z::Real
  parameter a::Real=10.0
  parameter b::Real=28.0
  parameter c::Real=8/3
relations
    der(x) = a * (y - x)
    der(y) = x * (b - z) - y
    der(z) = x * y - c * z
end
